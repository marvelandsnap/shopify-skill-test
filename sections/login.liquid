{%- capture section_content -%}
  {%- render 'utility.make-section-placeholder', filename: 'login.liquid', customers_template: true -%}
{%- endcapture -%}

{%- render 'utility.make-section', section_content: section_content -%}

{%- schema -%}
{
  "name": "Login",
  "tag": "section",
  "settings": [],
  "blocks": [],
  "presets": [
    {
      "name": "Login"
    }
  ],
  "enabled_on": {
    "templates": ["customers/login"]
  }
}
{%- endschema -%}
