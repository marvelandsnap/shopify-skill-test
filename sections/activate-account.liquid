{%- capture section_content -%}
  {%- render 'utility.make-section-placeholder', filename: 'activate-account.liquid', customers_template: true -%}
{%- endcapture -%}

{%- render 'utility.make-section', section_content: section_content -%}

{%- schema -%}
{
  "name": "Activate Account",
  "tag": "section",
  "settings": [],
  "blocks": [],
  "presets": [
    {
      "name": "Activate Account"
    }
  ],
  "enabled_on": {
    "templates": ["customers/activate_account"]
  }
}
{%- endschema -%}
