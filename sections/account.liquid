{%- capture section_content -%}
  {%- render 'utility.make-section-placeholder', filename: 'account.liquid', customers_template: true -%}
{%- endcapture -%}

{%- render 'utility.make-section', section_content: section_content -%}

{%- schema -%}
{
  "name": "Account",
  "tag": "section",
  "settings": [],
  "blocks": [],
  "presets": [
    {
      "name": "Account"
    }
  ],
  "enabled_on": {
    "templates": ["customers/account"]
  }
}
{%- endschema -%}
