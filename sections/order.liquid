{%- capture section_content -%}
  {%- render 'utility.make-section-placeholder', filename: 'order.liquid', customers_template: true -%}
{%- endcapture -%}

{%- render 'utility.make-section', section_content: section_content -%}

{%- schema -%}
{
  "name": "Order",
  "tag": "section",
  "settings": [],
  "blocks": [],
  "presets": [
    {
      "name": "Order"
    }
  ],
  "enabled_on": {
    "templates": ["customers/order"]
  }
}
{%- endschema -%}
