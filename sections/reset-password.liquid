{%- capture section_content -%}
  {%- render 'utility.make-section-placeholder', filename: 'reset-password.liquid', customers_template: true -%}
{%- endcapture -%}

{%- render 'utility.make-section', section_content: section_content -%}

{%- schema -%}
{
  "name": "Reset Password",
  "tag": "section",
  "settings": [],
  "blocks": [],
  "presets": [
    {
      "name": "Reset Password"
    }
  ],
  "enabled_on": {
    "templates": ["customers/reset_password"]
  }
}
{%- endschema -%}
